{
    "schemaVersion": 1,
    "title": "Google Maps Business Scraper Input",
    "type": "object",
    "description": "Configuration for scraping Google Maps businesses",
    "properties": {
        "searchQueries": {
            "title": "Search Queries",
            "type": "array",
            "description": "List of Google Maps search queries (e.g., 'plumbers in New York')",
            "editor": "json",
            "items": {
                "type": "string"
            },
            "example": ["plumbers in New York", "electricians in Los Angeles"],
            "prefill": ["plumbers in New York"]
        },
        "maxResultsPerQuery": {
            "title": "Max Results Per Query",
            "type": "integer",
            "description": "Maximum number of businesses to extract per search query",
            "editor": "number",
            "minimum": 1,
            "maximum": 50,
            "default": 20
        },
        "filterNoWebsite": {
            "title": "Filter: No Website Only",
            "type": "boolean",
            "description": "Only return businesses without a website (perfect for landing pages)",
            "editor": "checkbox",
            "default": true
        },
        "minRating": {
            "title": "Minimum Rating",
            "type": "number",
            "description": "Only return businesses with at least this rating (0-5)",
            "editor": "number",
            "minimum": 0,
            "maximum": 5
        },
        "minReviewCount": {
            "title": "Minimum Review Count",
            "type": "integer",
            "description": "Only return businesses with at least this many reviews",
            "editor": "number",
            "minimum": 0
        }
    },
    "required": ["searchQueries"]
}
